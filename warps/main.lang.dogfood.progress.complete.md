# Lang dogfood — observed integration

The former slice-1 current note is settled. Greeting fixtures passed against
the unchanged implementation. The inspector contract and red fixtures preceded
the adapter/routes; final fixture, real HTTP, browser and Julia regression
observations are retained in the per-slice verification files.

Recover acceptance with `recur warp show main.lang.dogfood -d warps` and
`recur warp slices main.lang.dogfood -d warps`. Read the linked walkthrough and
gap assessment from `main.lang.dogfood.readme.md`. Recorded completion here is
an attention marker; the live map/layers declare acceptance and the verification
records describe actual observations. No release publication is implied.

consumes: main.lang.dogfood.baseline observed starting behavior
produces: main.lang.dogfood.greeting-fixtures boundary acceptance examples
