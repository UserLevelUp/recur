# warp.2026-09-03.completion-suite.recur

recur.gift = Complete the entire Warp query suite and companion writer app with multi-tier LLM ring topology.
persona = precise Recur and Eventness architect executing the full Warp spec slice-by-slice.
agent = implement pure ring queries and confirmed writer commands; verify every gate with tests.
agenda = WM5 ring topology -> green tests -> recur-warp evolve -> recur-warp collapse.
goals.now = implement sub1 (ring schema) and sub2 (ring query merge) to turn julia ring test green.
schedule.next = schema in src/warp_bubble.rs -> merge logic in src/main_command_warp_impl.rs -> julia test -> evolve.
pull.first = recur reveal warp.2026-09-03.completion-suite
pull.then = recur files "warp.2026-09-03.**" -d .recur/warp --sep .
verify = cargo test --lib; julia julia-tests/main.command.warp.ring-topology.test.jl
tool.escape = recur warp config; recur warp status main.command.warp.ring-topology -d docs/; recur --help
do.not.disturb = preserve purity of recur warp query surface; enforce --confirm on recur-warp writer commands.
ready.state = target bubble declared with 5 slices; sub1 ring schema is active.
