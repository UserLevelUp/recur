# slice-4: consistent-list-presentation
Status: complete

Bare warp and list render trait-style sections, separate qualified counts,
escaped diagnostics and manifest scopes. Unknown counts remain unknown; ring
domain counts are separate. JSON inventory and query semantics remain stable.
The original discovery error-prefix assertion is preserved.

Acceptance gate: consistent-list-presentation.
Observed evidence: main.command.warp.identity-policy.verification.current.md.
