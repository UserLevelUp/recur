# Scoped discovery

Status: complete

Nearest config supplies discovery roots and excluded directory names. Include .recur. Manifest directory scopes isolate identities and evidence; preserve pure existing explicit queries. --scan-all bypasses discovery policy, not security containment. Test policy errors, scoped identities and hidden work.

Gate: verification

defines: recur.warp.project.discovery.slice.1 Scoped discovery


## Verified slice outcome

Implemented nearest-config discovery roots and excluded directory names; defaults include .recur and exclude fixtures/build output. Added --scan-all and effective policy JSON. Scope-qualified inventory uses local maps/layers and relative evidence, including recursive ring domains; legacy explicit query traversal unchanged. Focused CLI tests: 75 passed. Cargo: 179 passed, seven ignored docs. A fixture initially assumed helpers were already defined; corrected test ordering. Added fresh-checkout-safe real repository-root acceptance without depending on ignored private files.
